printf "Provisioning {{platform}}"
printf " on {{cloud}}"
printf " at {{domain}}"
{{#each services as |service|}}
  {{#if @first}}
    printf " with {{service}}"
  {{else}}
    printf " and {{service}}"
  {{/if}}
{{/each}}
echo " ..."

curl -sSLO "{{site}}/setup.sh"
chmod u+x setup.sh
./setup.sh
./terraform version
